<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:extractNativeLibs="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="ADBServer">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          exported="true" 是为了能接收 AutoJs6（另一个 App）发出的广播。
          注意：这意味着任何知道 action 名称的 App 理论上都能给它发命令，
          生产环境建议加自定义 permission 或校验发送方包名，见下方 Java 代码注释。
        -->
        <receiver
            android:name=".AdbBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.example.adbserver.RUN_COMMAND" />
            </intent-filter>
        </receiver>

        <service
            android:name=".AdbForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
